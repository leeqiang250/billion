spring:
  application:
    name: billion-spring-cloud-client
  profiles:
    active: dev
    #active: test
  cloud:
    config:
#      label: main
      profile: ${spring.profiles.active}
      uri: http://localhost:8888